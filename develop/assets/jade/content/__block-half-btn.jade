mixin half-btn( folder, num, text )
   article.block_article.block_article-half.block_btn
      .block_info.mrauto
         .block_icon( class='icon-#{folder}-#{num}')
         .text-18-dark !{text}
      .docs_btn
         a().button_blue Скачать

